[General]
network = BatmanDroneNetwork
repeat = 1

*.numRXHosts = 2  
*.numTXHosts = 1 
*.numRXTXHosts = 0
*.numRelays = 2

num-rngs = 3
**.mobility.rng-0 = 1
**.wlan[*].mac.rng-0 = 2

# Mobility Configuration
**.rxhost[*].mobility.typename = "StationaryMobility"
**.txhost[*].mobility.typename = "StationaryMobility"
**.rxtxhost[*].mobility.typename = "StationaryMobility"
**.relay[*].mobility.typename = "StationaryMobility"
**.mobility.constraintAreaMinZ = 0m
**.mobility.constraintAreaMaxZ = 0m
**.mobility.constraintAreaMinX = 0m
**.mobility.constraintAreaMinY = 0m
**.mobility.constraintAreaMaxX = 300m
**.mobility.constraintAreaMaxY = 300m
**.mobility.initFromDisplayString = false

# Application configuration
# RX hosts: 1 Batman app + apps to receive from TX and RXTX hosts
*.rxhost[*].numApps = 2
# TX hosts: 1 Batman app + apps to connect to RX and RXTX hosts
*.txhost[*].numApps = 3

*.relay[*].numApps = 1
**.txhost[0].mobility.initialX=50m
**.txhost[0].mobility.initialY=150m
**.rxhost[0].mobility.initialX=250m
**.rxhost[0].mobility.initialY=50m
**.rxhost[0].mobility.initialX=250m
**.rxhost[0].mobility.initialY=250m
**.relay[0].mobility.initialX = ${0,50,100,150,200,250,300}m
**.relay[0].mobility.initialY =${0,50,100,150,200,250,300}m
**.relay[1].mobility.initialX =${0,50,100,150,200,250,300}m
**.relay[1].mobility.initialY = ${0,50,100,150,200,250,300}m



# tcp settings
**.tcp.typename = "Tcp"
**.tcp.advertisedWindow = 65535                      # in bytes, corresponds with the maximal receiver buffer capacity (Note: normally, NIC queues should be at least this size)
**.tcp.delayedAcksEnabled = false                    # delayed ACK algorithm (RFC 1122) enabled/disabled
**.tcp.nagleEnabled = true                           # Nagle's algorithm (RFC 896) enabled/disabled
**.tcp.limitedTransmitEnabled = false                # Limited Transmit algorithm (RFC 3042) enabled/disabled (can be used for TcpReno/TcpTahoe/TcpNewReno/TcpNoCongestionControl)
**.tcp.increasedIWEnabled = false                    # Increased Initial Window (RFC 3390) enabled/disabled
**.tcp.sackSupport = false                           # Selective Acknowledgment (RFC 2018, 2883, 3517) support (header option) (SACK will be enabled for a connection if both endpoints support it)
**.tcp.windowScalingSupport = false                  # Window Scale (RFC 1323) support (header option) (WS will be enabled for a connection if both endpoints support it)
**.tcp.timestampSupport = false                      # Timestamps (RFC 1323) support (header option) (TS will be enabled for a connection if both endpoints support it)
**.tcp.mss = 1452                                    # Maximum Segment Size (RFC 793) (header option)
**.tcp.tcpAlgorithmClass = "TcpReno"                 # TcpReno/TcpTahoe/TcpNewReno/TcpNoCongestionControl/DumbTcp

# NIC settings
**.wlan[*].mgmt.frameCapacity = 10
**.wlan[*].mac.retryLimit = 7
**.wlan[*].mac.dcf.channelAccess.cwMin = 7
**.wlan[*].mac.cwMinMulticast = 31

**.wlan[*].radio.transmitter.power = 2mW
**.wlan[*].radio.centerFrequency = 2.4GHz
**.wlan[*].radio.receiver.sensitivity = -85dBm     # Better for drones
**.wlan[*].radio.receiver.energyDetection = -90dBm # 5dB below sensitivity
**.wlan[*].radio.receiver.snirThreshold = 4dB
**.wlan[*].opMode = "g(erp)"
**.wlan[*].mgmt.typename = "Ieee80211MgmtAdhoc"
**.wlan[*].agent.typename = ""
**.bitrate=18Mbps

*.radioMedium.pathLoss.typename = "FreeSpacePathLoss"
*.radioMedium.backgroundNoise.power = -86dBm



## RX HOSTS - Server applications
# RX hosts receive from TX hosts (apps 1 to numTX) - ports 10000+txhost_index
*.rxhost[*].app[1].typename = "TcpSinkApp"
*.rxhost[*].app[1].localPort = 10000 + ancestorIndex(0) - 1

## TX HOSTS - Client applications
# TX hosts connect to RX hosts (apps 1 to numRX)
*.txhost[*].app[1..2].typename = "TcpSessionApp"
*.txhost[*].app[1..2].sendBytes = 1000MiB
*.txhost[*].app[1..2].active = true
*.txhost[*].app[1..2].localPort = 20000
*.txhost[*].app[1..2].connectAddress = "rxhost[" + string(ancestorIndex(0) - 1) + "]"
*.txhost[*].app[1..2].connectPort = 10000 + ancestorIndex(1)
*.txhost[*].app[1..2].tOpen = 100s + uniform(0s, 1s)
*.txhost[*].app[1..2].tSend = 100s + uniform(0s, 1s)
*.txhost[*].app[1..2].tClose = -1s 
*.txhost[*].app[1..2].sendScript = ""


# Routing protocol
*.txhost[*].app[0].typename = "Batman"
*.rxhost[*].app[0].typename = "Batman"
*.relay[*].app[0].typename = "Batman"

**.batman.originatorInterval = 1s              # Standard interval
**.batman.broadcastDelay = uniform(0s, 0.08s)  # Reduced collision window  
**.batman.routeTimeout = 20s                   # Shorter timeout for mobility
**.batman.slidingWindowSize = 64               



cmdenv-express-mode = false
cmdenv-event-banners = false

**.app[0].cmdenv-log-level = detail

**.*.cmdenv-log-level = off

#**.cmdenv-log-level = off
**.vector-recording = false
**.app[1..].**.scalar-recording = true
**.scalar-recording = false
#**.cmdenv-log-level = off
cmdenv-redirect-output = true
cmdenv-log-prefix = "%t[%l]\t%C for %E: %|"

*.radioMedium.sameTransmissionStartTimeCheck = "ignore"

sim-time-limit = 200s



record-eventlog = false

